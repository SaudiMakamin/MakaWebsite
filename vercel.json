{
  "version": 2,
  "buildCommand": "npm run build && mkdir -p dist/public/attached_assets && cp -R attached_assets/* dist/public/attached_assets/ 2>/dev/null || true",
  "outputDirectory": "dist/public",
  "framework": null,
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "https://REPLIT_BACKEND_URL_PLACEHOLDER/api/:path*"
    },
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
